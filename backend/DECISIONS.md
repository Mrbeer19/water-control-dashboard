# DECISIONS.md — ข้อที่ backend ตัดสินเอง พร้อมเหตุผล

เรียงตามเวลา ข้อไหนถูกกลับคำตัดสินให้ขีดฆ่าแล้วเขียนข้อใหม่ ห้ามลบ

| # | เรื่อง | ตัดสินว่า | เหตุผล |
|---|---|---|---|
| D-01 | ชนิดของ id | ใช้ `TEXT` ชุดเดียวกับหน้าบ้าน (`zone-1`, `pump-1`) รวมถึง `zones.zone_id` ที่สเปกเขียนเป็น `SERIAL` | `lib/types.ts` ใช้ id เป็น string และ `lib/services/` ส่ง id นี้มาตรง ๆ ถ้าใช้ตัวเลขต้องมีตารางแปลงสองทางทุก endpoint |
| D-02 | ถังทรงเรขาคณิต (ถัง 1, ถัง VIP) | ใส่ `tank_profiles` 2 จุด (ว่าง, เต็ม) | ให้ทุกถังแปลงผ่าน `tank_volume()` ทางเดียว ไม่มีสูตร `level × area` แอบอยู่ในโค้ด · ถ้าวัดถังจริงแล้วไม่ตรงก็แค่เพิ่มจุดในตาราง |
| D-03 | index `(entity_id, time DESC)` ของ hypertable | ไม่สร้าง | PK `(entity_id, time)` เป็น btree ที่ scan ย้อนทางได้อยู่แล้ว สร้างซ้ำแค่เปลืองดิสก์และเขียนช้าลง |
| D-04 | การปัก image | ปักด้วย digest อย่างเดียว ไม่เขียน tag คู่ | ข้อห้ามเรื่อง tag `latest` · digest ชี้ของชิ้นเดียวแน่นอน ส่วนเวอร์ชันเขียนเป็นคอมเมนต์ |
| D-05 | entity ที่ไม่ได้อยู่ในรายการ 28 stream | เพิ่ม entity ของ โซน · วาล์ว · อุปกรณ์ · ระบบแรงดัน · `plant` | `alerts` / `state_spans` / `device_status` / `thresholds` มี FK ไป `entities` · 28 คือจำนวน **stream** ไม่ใช่จำนวน entity |
| D-06 | การแก้ schema | แก้ในไฟล์ `db/0x_*.sql` ได้ตรง ๆ **จนกว่าจะ deploy ครั้งแรก** | ยังไม่มีข้อมูลจริงที่ต้องย้าย · หลัง deploy แล้วต้องเพิ่มไฟล์ migration ใหม่เท่านั้น |
| D-07 | ACL ของ MQTT | ไฟล์ `mosquitto/acl` เขียนตรง ๆ ต่ออุปกรณ์ ตรงกับ seed | อุปกรณ์ส่งใน topic ของ entity (`tank/tank-1`) ไม่ใช่ของตัวเอง จึงใช้ `pattern %u` ไม่ได้ · เพิ่มอุปกรณ์ต้องแก้ acl + seed + รัน `gen_passwd.sh` |
| D-08 | branch | ทำใน `feature/pitchakorn-backend` branch เดียว commit แยกตามเฟส | เจ้าของงานขอ branch backend ก้อนเดียวไว้ push ขึ้น repo ทีม · ถ้าทีมอยากได้ PR ย่อยตาม `REPO_INTEGRATION.md` แตกจาก commit ของแต่ละเฟสได้ภายหลัง |
| D-09 | `rain_mm` ใน payload | ถือเป็น **ปริมาณต่อรอบส่ง** (`kind: amount`) | ตรงกับ `metric → kind` ใน HANDOFF §2.11 · ⚠️ ยังต้องยืนยันกับทีมฮาร์ดแวร์ (PROMPT_03) ถ้าเป็นค่าสะสมต้องเปลี่ยนเป็นคิดแบบ counter ภายใน |
| D-10 | `device_id` ของปั๊ม | ชี้ ESP32 ที่ **วัด** ค่า (PZEM) ส่วน PLC ที่ **สั่ง** เก็บใน `spec.controlledBy` | ตรงกับ `PumpSpec.deviceId` ในหน้าบ้าน · node วัดตาย = ปั๊ม offline ในมุมมองข้อมูล แม้ PLC ยังสั่งได้ |
| D-11 | ฟังก์ชันค่าน้ำ | `water_cost(m3, config)` คำนวณล้วน · `water_cost_at(m3, date)` เลือกอัตราตาม `effective_from` · `water_cost(m3)` = อัตราวันนี้ | แยกการคำนวณออกจากการเลือกอัตรา ทดสอบขั้นบันไดได้โดยไม่แตะตาราง และรายงานย้อนหลังเรียกด้วยวันที่ของข้อมูล |
| D-12 | ขอบขั้นบันได | ขั้นต่อเนื่อง `0–30, 30–50, …` (ไม่ใช่ `31–50`) | ตรงกับ `billing.tiers` ใน `lib/mock/settings.ts` · ปริมาณเป็นทศนิยม ช่วงเว้น 30–31 จะทำให้ 30.5 m³ ไม่อยู่ขั้นไหนเลย |
| D-13 | ชื่อ field ใน payload | กำหนดสัญญาใน `README.md` หัวข้อ "สัญญา payload" (`levelMeters`, `currentAmp`, `energyKwh`, `phases.L1` …) | PROJECT_BRIEF §7.1 ให้ตัวอย่างแค่ถัง · ⚠️ **ต้องให้ทีมฮาร์ดแวร์ยืนยัน** ถ้าเปลี่ยนชื่อ แก้ที่ `ingest/normalize.py` ที่เดียว และ ingest log `unknown_field` ให้เห็นทันทีเมื่อชื่อไม่ตรง |
| D-14 | `at` ที่ไม่มี offset | ทิ้งทั้งข้อความ + log `at_without_offset` | ไม่รู้ว่าเป็นเวลาเขตไหน เก็บไปก็ไปโผล่ผิดที่บนกราฟ 7 ชั่วโมง · ต่างจาก `clock_skew` ที่ offset ถูกแต่นาฬิกาเพี้ยน ซึ่ง**ยังเก็บ** |
| D-15 | ชื่อ `alerts.kind` | ใช้ `AlertCode` ใน `lib/types.ts` ตรง ๆ (`PUMP_OVERCURRENT`, `DEVICE_OFFLINE` …) ไม่ใช่ `overcurrent`/`offline` ตามตัวอย่างในเกณฑ์รับงาน | หน้าบ้านแปลข้อความจากรหัสนี้อยู่แล้ว ไม่ต้องมีตารางแปลงอีกชั้น |
| D-16 | liveness ทาง B | ตรวจ `last_seen` ในหน่วยความจำของ ingest ทุก 5 วินาที แทน query DB ทุก 15 วินาที | ผลเท่ากันแต่ยังทำงานตอน DB ล่ม และจับได้ใน 30–35 วินาที (query ทุก 15 วิจะช้าสุด 45 วิ) · ข้อจำกัด: ถ้ารัน ingest หลายตัวต้องย้ายไปใช้ DB/Redis |
| D-17 | อุปกรณ์ที่เฝ้า offline | เฝ้า ESP32 ทุกตัวตั้งแต่เริ่ม · PLC/HMI/gateway เฝ้าเมื่อเคยส่งข้อความมาแล้ว | PLC/HMI ไม่ได้ส่ง MQTT เอง ถ้าเฝ้าตั้งแต่ต้นจะมี `DEVICE_OFFLINE` ค้างตลอด |
| D-18 | ตอน ingest เริ่ม | ต้องโหลดทะเบียนจาก DB ได้อย่างน้อยครั้งแรก (วนรอ) · หลังจากนั้น DB ล่มได้โดยไม่หยุด | ไม่รู้จัก entity ก็ตรวจ/เขียนอะไรไม่ได้ · ⚠️ ข้อความที่อยู่ในคิวหน่วยความจำ (ยังไม่ถึง spool) หายถ้า process ถูก kill — spool ป้องกันกรณี DB ล่ม ไม่ใช่ ingest ล่ม |
| D-19 | ตู้ไฟ 3 เฟส | ส่งทุกเฟสในข้อความเดียว (`values.phases`) · เกณฑ์กระแสใช้ค่าสูงสุดของเฟสในข้อความนั้น | ถ้าส่งเฟสละข้อความ debounce 3 รอบจะครบในข้อความเดียวและนับการหายเป็นรายเฟสไม่ได้ |
| D-20 | simulator | ต่อ MQTT ด้วยรหัสของอุปกรณ์แต่ละตัว (`dev-*`) หนึ่ง connection ต่ออุปกรณ์ | ACL จริงทำงานระหว่างทดสอบ และ LWT เป็นของแต่ละ connection · S2–S12 ยิงข้อความเองใน pytest (แม่นกว่าให้ simulator ฉีด) ส่วน S1/S13/S14 ใช้ simulator |
| D-22 | ยอดรวมตู้ไฟ 3 เฟส | ingest เขียนแถว `phase = 'total'` เพิ่ม (W/kWh = ผลรวม ครบ 3 เฟสเท่านั้น · A = เฟสที่หนักสุด · V = เฉลี่ย) · aggregate และ API ใช้เฉพาะ `total`/`single` | min/max ของผลรวมทั้งตู้หาจาก min/max รายเฟสไม่ได้ ต้องรวม ณ เวลาเดียวกันตอนรับข้อมูล · ขาดเฟสไหนยอดรวมเป็น null ดีกว่าต่ำกว่าจริงแบบเงียบ ๆ |
| D-23 | เพดาน 1,000 จุดกับ `raw` | ใช้เพดานกับทุก granularity **ยกเว้น `raw`** ซึ่งถูกคุมด้วย PAIRING (≤ 1 ชม. = ไม่เกิน 1,800 จุด) | `PAIRING` ใน `time-buckets.ts` ให้ raw เป็นค่าเริ่มต้นของช่วง 1 ชม. ถ้าบังคับ 1,000 จุดกับ raw หน้าจอค่าเริ่มต้นจะได้ 400 · ⚠️ ต้องแจ้งหน้าบ้านใน `docs/BACKEND_CONTRACT_NOTES.md` |
| D-24 | ข้อมูลที่มาช้าเกินหน้าต่าง refresh | refresh policy ครอบ 14 วัน (5m) / 60 วัน (1h) / 365 วัน (1d) · นำเข้าข้อมูลเก่ากว่านั้นต้อง `CALL refresh_continuous_aggregate()` เอง | start_offset ต้องสั้นกว่า retention ของชั้นล่าง ไม่งั้น refresh จะลบ bucket ชั้นบนทิ้ง · spool/broker ล่มปกติไม่เกินหน้าต่างนี้ |
| D-25 | ลำดับไฟล์ schema | aggregate อยู่ `07_caggs.sql` · policy อยู่ `08_policies.sql` (สเปกเขียน 03/04) | `time_bucket` ต้องรู้เขตเวลาตอนสร้าง view และค่านี้อยู่ใน `settings` ซึ่งถูก seed ใน `06_seed.sql` · บันทึกเขตเวลาที่ใช้ไว้ใน `aggregate_config` ถ้า settings เปลี่ยนภายหลัง API ตอบ 409 แทนการส่งขอบวันผิด |
| D-21 | ค่าสะสมที่ส่งย้อนหลัง | ข้อความที่ `at` เก่ากว่าค่าล่าสุดของ entity ไม่ใช้ตรวจ counter reset และไม่เปลี่ยน state span | QoS 1 ส่งซ้ำ/มาช้าได้ ถ้าเอามาเทียบจะเกิด reset ปลอมและประวัติสถานะกลับด้าน · ผลข้างเคียง: เติมข้อมูลย้อนหลังด้วย simulator จะไม่มี state span ของช่วงนั้น |
| D-26 | ค่าตั้งระบบกับเกณฑ์/อัตรา | `settings` เก็บเฉพาะหมวดทั่วไป · เกณฑ์เตือนอ่านจากตาราง `thresholds` · ขั้นอัตราจาก `tariffs` · API ประกอบกลับเป็นรูป `SystemSettings` (เฟส 4c) | ingest ตรวจเกณฑ์และการคิดค่าน้ำใช้สองตารางนี้อยู่แล้ว เก็บซ้ำใน settings จะมีสองต้นทางที่ไม่ตรงกัน · สำเนาใน `Tank.thresholdsPercent` / `Zone.thresholdsLpm` มาจากตารางเดียวกันเสมอ |
| D-27 | entity ที่ยังไม่มีข้อมูล | field ที่สัญญาบังคับ `number` ตอบ 0 · `lastSeen` = epoch 0 · และ `status = 'offline'` เสมอ · field ที่เป็น `number \| null` ตอบ null ตามจริง | `lib/types.ts` ไม่ยอมให้ null ในฟิลด์เหล่านี้ · หน้าบ้านตัดสินจาก status ก่อน (สีเทา) จึงไม่มีใครอ่าน 0 เป็นค่าจริง |
| D-28 | ตำแหน่งวาล์วที่ยังไม่เคยได้ feedback | ตอบ `'open'` (สถานะตั้งต้นของโรงงาน) · `cycleCount` นับจาก span `closed` | ingest ยังไม่รับ feedback วาล์ว (ทำพร้อมระบบสั่งงานเฟส 5) · วาล์วโซนเปิดค้างเป็นปกติ ถ้าตอบ `fault` จะเตือนทุกโซนตลอด |
| D-29 | ระบบควบคุมแรงดัน | setpoint / gain / mode อ่านจาก `entities.spec` (ค่าตอนติดตั้ง) · แรงดันจริงและ output % มาจากปั๊มที่ลูปคุม (`vfd_hz / 50`) | ยังไม่ได้อ่าน DB ของ PLC · ⚠️ ถ้าช่างแก้ค่าที่ HMI หน้าจอจะไม่เห็นจนกว่าจะเชื่อม S7 (เฟส 5) |
| D-30 | PLC/HMI/gateway ในหน้า Devices | PLC/HMI เป็น `offline` จนกว่าจะมีข้อความสถานะ · gateway = `ok` และ `lastSeen` = ตอนนี้ (API รันบนเครื่องนี้) · ping ตัดสินจากข้อความล่าสุด ไม่ส่ง ICMP · reboot/firmware ตอบ 501 | PLC/HMI ไม่ได้ส่ง MQTT เอง (D-17) · container ไม่มีสิทธิ์ raw socket และอยู่คนละ VLAN · คำสั่งถึงอุปกรณ์ต้องผ่าน audit log (เฟส 5) |
| D-31 | `/api/meters/daily?projection=true` | ยังไม่ต่อจุดอนาคต (`projected` เป็น false ทุกจุด) | ผลพยากรณ์เป็นของทีม AI (เฟส 4e) · backend ห้ามคิดสูตรพยากรณ์เอง |
| D-32 | ยอด "วันนี้ / เดือนนี้" บนการ์ดสด | ค่าล่าสุด − ค่าสุดท้ายก่อนเที่ยงคืน (2 index lookup) · รายงานและรอบบิลใช้ผลรวม delta จาก aggregate แบบเดียวกับ `/api/metrics/series` | การ์ดถูกดึงทุก 2 วินาที รวม bucket ทุกครั้งเปลืองเกินจำเป็น · ข้อจำกัด: ตัวนับรีเซ็ตเกิน 1 ครั้งในวันเดียว การ์ดจะต่ำกว่ากราฟ ส่วนรายงานถูกเสมอ |
| D-33 | `/…/history` | bucket ตาม `interval` วินาที (รับ `intervalSeconds` ด้วย) (ค่าเริ่มต้นให้ได้ราว 1,000 จุด · ขั้นต่ำ 2 วินาที) · เพดาน 2,000 จุด · ช่วงเริ่มต้น 1 ชม. ล่าสุด · `from`/`to` รับทั้ง epoch ms และ ISO ที่มี offset | สัญญาเดิมของ sparkline (`TimeSeriesPoint[]`) คนละ endpoint กับ `/api/metrics/series` ห้ามยุบรวมกัน |
| D-34 | น้ำสูญหายช่วงที่เริ่มก่อนมีข้อมูล | Δstorage ใช้ปริมาตรค่าแรกในช่วงแทนค่าก่อนช่วง | ตัวนับมิเตอร์ก็เริ่มจากค่าแรกในช่วงเหมือนกัน · ถ้าไม่ทำ Δstorage = 0 แล้วระบบเตือนรั่ว 15% ตอนเติมถัง (เจอตอนทดสอบเฟส 4a) |
| D-35 | alert ที่เกิดซ้ำ | writer เปิดแถวเดิมเมื่อเหตุเดิมกลับมาภายใน `notifications.deduplicationWindowMinutes` (`occurrence_count` +1 · severity ขึ้นได้ไม่ลง) · notifier ไม่ส่งรอบใหม่ | ตรงกับความหมายของ `Alert.occurrenceCount` และค่าตั้งกันสแปมของหน้าบ้าน · จุดวัดที่แกว่งรอบเกณฑ์จะไม่ยิงบัซเซอร์ทุกไม่กี่นาที |
| D-36 | ตัวส่งแจ้งเตือน | service แยก `notifier` (`python -m api.notifier` image เดียวกับ api) อ่าน DB ทุก 2 วินาที ใช้ `notification_log` เป็นคิว (`FOR UPDATE SKIP LOCKED`) | ingest ไม่ต้องรู้เรื่องช่องทาง · api ล่มแจ้งเตือนยังออก · รีสตาร์ตแล้วงานค้างไม่หาย · 2 วินาทีเร็วพอสำหรับบัซเซอร์ |
| D-37 | กฎการส่ง | ส่งถึงทุกผู้รับใน `recipients[channel]` (mock ส่งคนแรกคนเดียว) · ช่วงเงียบ = เลื่อนไปส่งเมื่อพ้นช่วงถ้ายังไม่หาย · ไม่มีคนรับทราบเกิน `escalationAfterMinutes` = ส่งซ้ำ 1 รอบ · snooze ครบแล้วยังไม่หาย = เตือนซ้ำ 1 รอบ · ย้อนดู alert ที่เปิดไม่เกิน 24 ชม. | ⚠️ `notifyDepartmentManager` ยังไม่ทำ — ตาราง `users` ยังไม่มีอีเมล/ช่องทางติดต่อของหัวหน้าแผนก |
| D-38 | ช่องทางส่ง | email = SMTP ภายใน (`SMTP_HOST` ใน .env) · buzzer = MQTT `plant/water/buzzer/cmd` ด้วย user `api` (ACL เดิมอนุญาต `plant/water/+/cmd`) · webhook เฉพาะปลายทาง IP ส่วนตัว · line/sms ล้มทันทีพร้อมเหตุผล | ตาม PROBLEMS P-06 · ข้อผิดพลาดถาวร (ไม่ได้ตั้งค่า / ปลายทางปฏิเสธ) ไม่ลองซ้ำ · ชั่วคราวลองใหม่หลัง 30 วินาที และ 2 นาที รวม 3 ครั้ง |
| D-39 | `Alert.state` | คิดจากข้อมูล: ปิดแล้ว = `resolved` · มีการรับทราบ = `acknowledged` · นอกนั้น `active` · ไม่มีคอลัมน์ state | ingest ปิด alert ได้โดยไม่ต้องรู้เรื่องการรับทราบ และไม่มีทางที่สองค่าจะขัดกัน · รับทราบไม่ทำให้ alert ปิด (รับทราบ ≠ ปัญหาหาย) |
| D-40 | ผู้รับทราบ | ชั่วคราวรับ `acknowledgedByUserId` จาก body · ตรวจว่าเป็น operator/admin · ลง `audit_log` ทุกครั้ง | สัญญาเดิมของ `acknowledgeAlert()` · ⚠️ เฟส 4c ต้องเปลี่ยนเป็นผู้ใช้จาก session และตรวจสิทธิ์ endpoint ที่เขียนข้อมูลทั้งหมด (✅ ทำแล้ว — D-41) |
| D-41 | สิทธิ์ของ endpoint | อ่านทั้งหมด (รวม `GET /api/settings`) ไม่ต้องล็อกอิน · อ่านแล้ว/อ่านทั้งหมด = ผู้ที่ล็อกอิน · รับทราบ/ส่งซ้ำ = operator ขึ้นไป · แก้/ส่งออก/นำเข้า/ทดสอบค่าตั้ง = admin · ผู้รับทราบคือผู้ที่ล็อกอิน (ไม่เชื่อ id ใน body) | จอแขวนผนังในห้องคอนโทรลต้องเปิดค้างได้โดยไม่ต้องล็อกอิน · ⚠️ `departmentScopedAccess` ยังไม่บังคับ — ต้องให้ทีมตัดสินว่าจอแขวนผนังนับเป็นผู้ใช้แบบไหนก่อน |
| D-42 | เซสชัน | token สุ่ม 256 bit ใน cookie `wcm_session` (httpOnly · SameSite=Strict · path=/api) · DB เก็บ sha256 · หมดอายุตาม `sessionTimeoutMinutes` ต่ออายุได้ไม่เกิน 12 ชม. จากตอนล็อกอิน · logout ยกเลิกที่เซิร์ฟเวอร์ | SameSite=Strict กัน CSRF โดยไม่ต้องมี token เพิ่ม · ⚠️ ตั้ง `COOKIE_SECURE=1` เมื่อเปิด https · ⚠️ ตอนพัฒนา หน้าบ้าน :3000 กับ API :8000 เป็นคนละ origin ต้องผ่าน proxy (เฟส 7) |
| D-43 | ล็อกอินไม่ผ่าน | ข้อความเดียวกันทุกกรณี · ไม่มีผู้ใช้ก็เทียบ hash หลอกให้เวลาตอบเท่ากัน · ผิด 5 ครั้งใน 15 นาทีต่อชื่อผู้ใช้ (รวมชื่อที่ไม่มีจริง) ล็อกชื่อนั้น 15 นาที · ตอบ 200 + `ok: false` | กันเดาชื่อผู้ใช้และเดารหัส · 200 เพราะสัญญา `signIn()` คืน SignInResult ไม่ throw · ⚠️ คนในเครือข่ายแกล้งล็อกชื่อคนอื่นได้ 15 นาที — ยอมรับได้ใน LAN โรงงาน |
| D-44 | รหัสผ่าน | seed ไม่มีรหัส · ตั้งด้วย `make password NAME=…` (argon2id · อย่างน้อย 10 ตัว) · ตั้งใหม่แล้วเซสชันเดิมถูกยกเลิก · สคริปต์อยู่ `api/set_password.py` (สเปกเขียน `scripts/`) | argon2 อยู่ใน image ของ api เครื่องติดตั้งไม่ต้องลงอะไรเพิ่ม · ห้ามมีรหัสตั้งต้นใน repo · เทสตั้งรหัสสุ่มใหม่ทุกรอบผ่านสคริปต์ตัวจริง |
| D-45 | ค่าตั้งต้นของ reset | `06_seed.sql` คัดลอก settings / thresholds / tariffs ลงตาราง `*_factory` ตอนติดตั้ง · reset คืนจากตารางนี้ (อัตราคืนเป็นแถวที่มีผลวันนี้) · ไม่ลบ LINE token | ต้นทางค่าตั้งต้นมีที่เดียว ไม่ต้องดูแลค่าตั้งต้นอีกชุดใน Python · ⚠️ ต่างจาก `DEFAULT_SETTINGS` ของ mock โดยตั้งใจ: ช่องทาง LINE ปิดอยู่ (P-06) |
| D-46 | กฎตรวจค่าตั้ง | `validateSettings()` พอร์ตตรงตัว ตรวจกับค่าอ้างอิงจากไฟล์ .ts จริง (`make settings-golden`) · กฎเพิ่มเฉพาะเซิร์ฟเวอร์: ชนิดข้อมูล/ฟิลด์ที่ไม่รู้จัก · enum · HH:mm · ลำดับเกณฑ์ · นาทีเป็นจำนวนเต็ม · เขตเวลา | กฎชุดเดียวกับหน้าจอตาม PROMPT_04 · กฎที่เพิ่มกันค่าที่ทำระบบพังจริง เช่นหน้าต่างกันสแปมเป็นทศนิยมจะทำ SQL ของ ingest ล้ม |
| D-47 | เปลี่ยนเขตเวลา | ปฏิเสธ offset ที่ไม่ใช่ชั่วโมงเต็มในเดือนใดก็ตาม และปฏิเสธเขตที่ต่างจาก `aggregate_config` | ข้อหลังกันกราฟพังทั้งระบบ (series ตอบ 409) · เปลี่ยนจริงต้องสร้าง aggregate ใหม่ก่อน |
| D-48 | เกณฑ์ในหมวดรายอุปกรณ์ | `tanks[].thresholdsPercent` และเกณฑ์ใน `environment[]` อ่านจากตาราง thresholds เสมอ · แก้เกณฑ์ที่หมวด `thresholds` เท่านั้น | ต้นทางเดียว (ThresholdSettings เป็นต้นทางความจริงตาม `lib/types.ts`) · ingest เห็นค่าใหม่ภายใน 60 วินาที |
| D-49 | `/api/settings/line/test` | ตอบ `NotificationTestResult` ที่ `ok: false` และ `message` ขึ้นต้นด้วย `offline_mode` (สเปกเขียน `{ ok, reason }`) | ของที่มีในโค้ดชนะสเปก — type ของหน้าบ้านไม่มีฟิลด์ `reason` |
| D-50 | รูปข้อความ WS | หนึ่งข้อความ = อาร์เรย์ `RealtimeEvent[]` (entity ทั้งก้อน) · ไม่เพิ่ม type `tick` | ได้ทั้ง "ไม่เกิน 1 ครั้งต่อ 2 วินาที" (P-02) และ "ส่ง entity เต็มก้อน" (P-03) ด้วย type ที่มีอยู่แล้ว · `useLiveData` ที่ listener ไม่รับพารามิเตอร์ใช้ได้ทันที (หนึ่งข้อความ = โหลดหนึ่งครั้ง) · ⚠️ หน้าบ้านยังต้องยืนยัน P-03 |
| D-51 | ส่งเมื่อเปลี่ยนจริง | เทียบ fingerprint ของ payload โดยไม่นับ `lastSeen` / `updatedAt` / `lastSyncAt` / `latencyMs` · alert ส่งทุกครั้งที่มีคนแจ้ง · ไม่มี client = ไม่ประกอบ entity | ค่าไม่ขยับก็ไม่ส่งซ้ำ · ⚠️ เวลา "อัปเดตล่าสุด" บนจอให้ใช้จาก REST ที่โหลดใหม่ ไม่ใช่จาก WS |
| D-52 | จังหวะส่ง | hub ประกอบทุก 2 วินาที · แต่ละ client บังคับห่างอย่างน้อย 2 วินาทีเอง · ค้างส่ง = รวมก้อนเดียวด้วยค่าล่าสุด · ไม่ต้องล็อกอิน (D-41) | วัดจริง: ingest 15.5 แถว/วินาที → 10 ข้อความใน 20 วินาที ห่างน้อยสุด 2.017 วินาที · client ช้าไม่ถ่วงคนอื่น · ⚠️ ถ้ารัน api หลาย process ต้องย้าย fingerprint ไป Redis |
| D-53 | channel ของอุปกรณ์ | event `device` อยู่ channel `system` คู่กับ `connection` · `commands` ยังว่างจนถึงเฟสระบบควบคุม | `RealtimeChannel` มี 4 ค่า และสถานะอุปกรณ์เป็นเรื่องสุขภาพระบบ ไม่ใช่ค่าวัด |
| D-54 | ทางรับผลจากทีม AI | บริการ AI ส่งเข้า `POST /api/ai/anomalies` `/forecasts` `/maintenance` `/metrics` และ `PUT /api/ai/status` ด้วย `Authorization: Bearer <AI_INGEST_TOKEN>` (หรือ admin ที่ล็อกอิน) · `id` ของทีม AI เก็บเป็น `external_id` ส่งซ้ำ = อัปเดตแถวเดิม · `id` ใน API เป็นเลขของ backend | PROMPT_04 กำหนดแค่ทางออก ต้องมีทางเข้าที่ตรวจสัญญาได้ · token ว่าง = ปิดทางนี้ ไม่ใช่เปิดให้ทุกคน |
| D-55 | `sourceId` ของความผิดปกติ | บังคับและต้องเป็น entity ที่มีจริง (FK) · ไม่ส่ง `sourceType` มาเติมจากทะเบียน ส่งมาไม่ตรง = ปฏิเสธ · `targetId` ของ maintenance และ `scopeId` ของ metrics ตรวจแบบเดียวกัน | PROMPT_04 และหมุดบนกราฟต้องมี sourceId · ⚠️ ขัดกับ `docs/AI_CONTRACT.md` ที่เขียนว่า sourceId เป็น optional — ต้องแจ้งทีม AI |
| D-56 | score / healthScore นอกช่วง | ปฏิเสธพร้อมบอกวิธีแก้ ไม่หาร 100 ให้เอง | 0.8 กับ 80 แยกไม่ออกถ้าเดา · สัญญาเขียนให้ทีม AI หารก่อนส่ง · DB มี CHECK กันซ้ำอีกชั้น |
| D-57 | ของที่คนตัดสินแล้ว | `status` / `resolvedAt` / `feedback` ที่คนตั้งแล้ว ทีม AI ส่งซ้ำไม่ทับ · field ที่ทีม AI ไม่ได้ส่งมาจะไม่มีใน response | หลักการข้อ 3 ของสัญญา (ไม่เติมค่าปลอม) · กันโมเดลเปิดเคสที่หน้างานปิดไปแล้วกลับขึ้นมาใหม่ |
| D-58 | ความผิดปกติ → alert | เคสที่ยังเปิดและ severity ≥ warning เปิด alert `ANOMALY_DETECTED` ของ entity นั้น (หลายเคสรวมเป็น alert เดียว นับ occurrence) · ปิดครบทุกเคสที่ผูกไว้ → alert ปิด · stream ส่ง event `anomaly` ใน channel `alerts` | ผ่านกฎแจ้งเตือนชุดเดียวกับเหตุอื่น (ช่วงเงียบ · ยกระดับ) · ทีม AI ไม่ต้องสร้าง alert เอง |
| D-59 | สถานะบริการ AI | `reachable` = ได้สัญญาณชีพภายใน 5 นาที · `lastResultAt` = ผลล่าสุดที่รับไว้ทุกชนิด · `GET /api/ai/forecast?target=` ที่ยังไม่มีผล = 404 | backend ไม่รู้ว่าบริการ AI จะรันที่ไหน ให้บริการรายงานเองชัดกว่าไปเดา · `getForecast()` คืน `AIForecast` ไม่ใช่ `null` จึงตอบ 404 แทน |
| D-60 | ระดับน้ำต้นทางกับการสตาร์ตปั๊ม | ใช้ค่าล่าสุดที่เคยได้แม้อุปกรณ์เพิ่งหลุด · ถังที่ไม่เคยมีค่าเลยไม่ห้าม | ต่ำก็คือต่ำ — รุ่นแรกยกเว้นถังที่ offline แล้วเทสเกณฑ์ข้อ 8 จับได้ว่าปั๊มสตาร์ตได้ตอนเซนเซอร์เพิ่งหลุด · ไม่เคยมีค่าเลยถ้าห้ามจะสั่งปั๊มไม่ได้ระหว่างติดตั้ง และปั๊มยังมี level switch ใน PLC |
| D-61 | กฎ interlock ใน DB | `interlock_rules` เก็บ `check_name` · `actions` · `params` · ข้อความ · เปิด/ปิด — โค้ดมีวิธีตรวจ 15 แบบ · ชื่อที่ไม่รู้จัก = ระงับคำสั่ง (fail-closed) · ตัดสินทีละคำสั่งภายใต้ `pg_advisory_xact_lock` | PROMPT_05 ให้โหลดกฎจากตาราง · เงื่อนไขอิสระ (SQL/สคริปต์) ใน DB อันตรายกว่าและทดสอบไม่ได้ · lock ทำให้ปิดวาล์ว 6 ตัวพร้อมกันนับเพดานได้ถูก (วัดจริง ผ่าน 3 ปฏิเสธ 3) |
| D-62 | ยืนยันสองชั้น | เซิร์ฟเวอร์ตอบ 409 `CONFIRMATION_REQUIRED` พร้อม `details.confirmToken` (120 วินาที · ใช้ครั้งเดียว · ผูกผู้ใช้ + เป้าหมาย + คำสั่ง + ค่า) หน้าจอส่งคำสั่งเดิมกลับพร้อมโทเคน · คำสั่งอัตโนมัติใช้โทเคนไม่ได้ | กล่องยืนยันในหน้าจออย่างเดียว curl ข้ามได้ · ⚠️ หน้าบ้านต้องเพิ่ม `confirmToken` ใน `issueCommand()` (เฟส 7) |
| D-63 | PIN สั่งงาน | `users.pin_hash` (argon2) ตั้งด้วย `make password NAME=… PIN=1` · `POST /api/control/unlock` ใส่ครั้งเดียวสั่งได้ 15 นาทีในเซสชันนั้น หรือแนบ `pin` มากับคำสั่ง · ผิด 5 ครั้งใน 15 นาทีระงับการสั่งงานของผู้ใช้ | `PinGate` เดิมตรวจที่หน้าจอ · ⚠️ หน้าบ้านต้องเรียก `/unlock` แทน |
| D-64 | สถานะคำสั่ง → `CommandState` | pending → `sending` · awaiting_feedback · confirmed → `success` · timeout → `timeout` · rejected → `failed` + `errorCode` (rule_id · CONFIRMATION_REQUIRED · BROKER_UNAVAILABLE · DEVICE_REJECTED) | `types.ts` ไม่มีสถานะ rejected · timeout แยกชัดเป็น "ไม่ทราบผล" ตามเหตุผลเรื่องค้อนน้ำ |
| D-65 | feedback จากอุปกรณ์ | `{commandId, ok, position \| runState \| mode \| value, error}` · ไม่มี commandId = จับคำสั่งล่าสุดที่รอของเป้าหมายนั้น · ตำแหน่งวาล์วลง `state_spans` แม้ไม่มีคำสั่ง (ช่างหมุนที่ตู้เอง) · latency = ตั้งแต่บันทึกคำสั่งจนได้ feedback | ⚠️ ต้องให้ทีมฮาร์ดแวร์ยืนยันรูป payload (contract notes ข้อ 39) |
| D-66 | หยุดฉุกเฉิน | ล็อกการสั่งงานในซอฟต์แวร์ก่อนส่งคำสั่ง แล้วส่ง stop ถึงปั๊มทุกตัวเป็นคำสั่งย่อย · ไม่ต้องยืนยันสองชั้นและไม่ติดกฎใด · ปลดล็อกคืนโหมดเดิมของปั๊ม | หยุดคือทิศทางที่ปลอดภัย ห้ามมีอะไรขวาง · clear-lockout ไม่ได้สั่งอุปกรณ์จึงบันทึกว่ายืนยันทันที |
| D-67 | ปุ่มเปิด/ปิดวาล์วทุกโซน | ต้องยืนยันสองชั้น · คำสั่งย่อยไม่นับเพดานปิดวาล์ว · ตั้งเป็นตารางเวลาไม่ได้ | ⚠️ ปิดทุกโซนพร้อมกันเสี่ยงค้อนน้ำ ยังไม่ได้หน่วงทีละตัว — ต้องให้ทีมตัดสินว่าจะเก็บปุ่มนี้ไว้ไหม |
| D-68 | รีบูต · เฟิร์มแวร์ | รีบูตส่ง `plant/water/<device>/cmd` เฉพาะ ESP32 · `ok` = ส่งออก ไม่ใช่รีบูตสำเร็จ · firmware ยังตอบ 501 | สัญญา status ไม่มีเวอร์ชันเฟิร์มแวร์ จึงยืนยันผล OTA ไม่ได้ |
| D-69 | ตารางเวลา | dispatcher เดินทุก 10 วินาทีตามเวลาโรงงาน · พลาดรอบ (dispatcher ดับ) ทำครั้งเดียวแล้วไปรอบถัดไป · ตั้งตารางที่ติดกฎตายตัว (ปิดโซน VIP) ไม่ได้ และถึงเวลาจริงก็ตรวจซ้ำ | ป้องกันสองชั้น — เกณฑ์ข้อ 2 ทดสอบกับแถวที่ใส่ลง DB ตรง ๆ |
| D-70 | ค่าน้ำรายวัน · รายโซนในรายงาน | ขั้นบันไดคิดจากยอดรวมทั้งช่วง (รายงาน) หรือทั้งรอบบิล (รายวัน) แล้วเฉลี่ยลงตามสัดส่วนปริมาณ · `/api/meters/daily` เปลี่ยนมาใช้ตัวเดียวกัน · `/api/zones/cost` ยังคิดขั้นบันไดแยกรายโซนตามหน้าบ้านเดิม | บิลจริงคิดขั้นบันไดจากยอดทั้งรอบ ถ้าคิดทีละวัน/ทีละโซน ทุกก้อนตกขั้นแรกและผลรวมต่ำกว่าบิล · ⚠️ ค่าน้ำรายโซนสองหน้าจอจึงไม่เท่ากัน ต้องให้ทีมเลือกแบบเดียว |
| D-71 | อัตราของรายงานย้อนหลัง | ใช้อัตราที่มีผล ณ วันเริ่มของช่วงทั้งช่วง (รอบบิล · เดือน · รอบจดมิเตอร์เริ่มที่วันจดครั้งก่อน) ไม่ผสมอัตรา | ประปาคิดทั้งรอบด้วยอัตราเดียว · เกณฑ์ข้อ 4 ทดสอบด้วยการขึ้นอัตรากลางรอบ |
| D-72 | ขอบช่วงของรายงาน | `from`/`to` ขยายเป็นวันเต็มตามเวลาโรงงาน (00:00 ถึง 00:00 วันถัดไป) · ช่วงก่อนหน้า = ยาวเท่ากันติดกัน · สูงสุด 400 วัน (`leak_audit` 93 วัน) | ตัวนับรวมรายชั่วโมง/รายวันต้องเริ่มตรงขอบ bucket ไม่งั้นยอด bucket แรกเกินช่วง · `leak_audit` คิดน้ำสูญหายทีละวัน หนักกว่ารายงานอื่นมาก |
| D-73 | ค่าไฟใน `BillingEstimate` | ขั้นเดียว (อัตรา + Ft) ใน `tierBreakdown` · ไม่มีค่าบริการ · VAT ใช้อัตราจากตารางค่าน้ำ · ยอดใช้ = ผลรวมตู้ไฟทุกแผนก · kWh อยู่ในฟิลด์ชื่อ `consumedCubicMeters` | สัญญามี type เดียวสำหรับทั้งสองอย่าง · ⚠️ ตาราง tariffs ของค่าไฟยังไม่มี VAT/ค่าบริการของตัวเอง |
| D-74 | ตัวนับของ simulator | เก็บตัวนับสะสมลง volume `sim-state` ทุกรอบ รีสตาร์ตแล้วนับต่อ (แทน NVS ของบอร์ด) | รุ่นเดิมเริ่มจากค่าใน profile ทุกครั้ง ตัวนับถอยหลังแล้วถูกนับเป็นรีเซ็ต — ทดสอบรายงานเจอโซน 8 ใช้น้ำ 8,030 ลบ.ม. ใน 18 นาที · ข้อจำกัดของ `delta = last(N) − last(N−1)`: รีเซ็ตภายใน bucket หยาบมองไม่เห็น ยอดรายวันกับรายชั่วโมงต่างกันได้เฉพาะช่วงที่ตัวนับรีเซ็ตจริง |
| D-75 | รายงาน `energy` | แถวตู้ไฟแต่ละแผนก + แถวมิเตอร์ปั๊ม (ป้าย "มิเตอร์ย่อย") · ยอดรวมและสัดส่วนคิดเฉพาะตู้ไฟ | ⚠️ ยังไม่รู้ว่าปั๊มรับไฟจากตู้ไหน ถ้ารวมปั๊มในยอดรวมอาจนับซ้ำ — ต้องให้ทีมไฟฟ้ายืนยัน |
| D-76 | บันทึกการจดมิเตอร์ | อ่านจาก `meter_readings` · หน่วยที่ออกบิล = เลขครั้งนี้ − ครั้งก่อน (ติดลบ = เปลี่ยนมิเตอร์ นับจากเลขใหม่) · ยังไม่มี endpoint บันทึกค่า | `lib/services/reports.ts` มีแค่ฝั่งอ่าน · ⚠️ ต้องตกลงว่าใครบันทึกเลขจดของประปา (หน้าจอ admin หรือนำเข้าไฟล์) |
| D-77 | worker น้ำสูญหาย | ประเมินทุกขอบ 5 นาที หน้าต่าง 60 นาทีย้อนหลัง (รอ 1 นาทีหลังขอบให้ ingest เขียนครบ) · เก็บ `plant_metrics` → metric `system` / `unaccounted_percent` · เปิด/ปิด alert `UNACCOUNTED_WATER_HIGH` ของ entity `plant` ตามเกณฑ์ในตาราง thresholds (8 / 15%) ด้วย SQL ชุดเดียวกับ ingest | หน้าต่างสั้นกว่านี้ การปัดเศษเซนเซอร์ระดับบ่อ (0.001 ม. ≈ 148 ลิตร) ทำ % แกว่ง · ⚠️ รั่วที่เริ่มขึ้นจะเตือนช้าสุดราว 1 ชั่วโมง |
| D-78 | ข้อมูลไม่ครบ · น้ำเข้าน้อย | มิเตอร์หรือถังตัวใดไม่มีข้อมูลในหน้าต่าง = ไม่ตัดสิน (% เป็น null · ไม่เปิดไม่ปิด alert) · น้ำเข้ามิเตอร์หลักน้อยกว่า 1 ลบ.ม. ในหน้าต่าง = เก็บ % แต่ไม่เตือน | มิเตอร์โซนหลุดตัวเดียว Σโซนจะต่ำลงแล้วเตือนรั่วปลอมทั้งโรงงาน · ปิด alert ตอนข้อมูลหายจะทำให้เหตุที่ยังอยู่หายไปจากจอ |
| D-79 | ระดับถังตั้งต้นของสถานการณ์ | เพิ่มสถานการณ์ `pond_fill` (บ่อสำรองเริ่ม 89%) · `night_leak` เริ่มถัง 1 ที่ 94% | ถัง 1 เริ่ม 68% ประปาไหลเข้าราว 260 L/min รั่ว 11.4 L/min เห็นแค่ราว 4% ไม่ถึงเกณฑ์ · กลางคืนจริงถังเกือบเต็ม · บ่อเริ่ม 92% สูงกว่าจุดเริ่มเติมจึงไม่เคยเติมระหว่างทดสอบ |
| D-80 | ส่งออกรายงาน | `POST /api/reports/export` สร้างงาน (ต้องล็อกอิน ทุก role) → worker เรนเดอร์ทุก 2 วินาที → `GET /api/reports/export/:jobId` ส่งไฟล์ (202 ระหว่างรอ · 422 ล้มเหลว · 410 ไฟล์เกิน 7 วัน) · `xlsx` ตอบ 400 `FORMAT_NOT_SUPPORTED` | ไฟล์ออกจากตัวเลขชุดเดียวกับ `GET /api/reports` · xlsx ต้องเพิ่ม dependency ขณะที่ CSV เปิดใน Excel ได้อยู่แล้ว · ⚠️ `requestReportExport()` ยังไม่มีทางดาวน์โหลดไฟล์ หน้าบ้านต้องเรียก GET ตาม jobId (เฟส 7) |
| D-81 | image ของ worker | แยก image (`docker/worker.Dockerfile` · build context = backend/ ส่งเฉพาะ api/ กับฟอนต์) · Pango/HarfBuzz จาก apt ตอน build + WeasyPrint 65.1 + pyarrow 21 · ฟอนต์ Sarabun (OFL) จาก `assets/fonts` แล้ว `fc-cache` · หาฟอนต์ไม่เจอ build ล้ม | image ของ api ไม่ต้องแบกไลบรารี PDF (worker 604 MB) · ⚠️ แพ็กเกจ apt ไม่ได้ปักเวอร์ชัน (ฐานปัก digest แล้ว) — ชุดติดตั้งออฟไลน์ต้อง build ครั้งเดียวแล้ว `docker save` |
| D-82 | รูปไฟล์ส่งออก | CSV: BOM · metadata 5 แถว · null = ช่องว่าง · ตัวเลขดิบไม่มีตัวคั่นหลักพัน · PDF: A4 แนวนอน หัวตารางซ้ำทุกหน้า · null = "–" · เก็บ 7 วันใน volume `exports` | ตัวคั่นหลักพันทำให้ Excel อ่านเป็นข้อความคำนวณต่อไม่ได้ · ไฟล์เป็นของชั่วคราว ข้อมูลจริงอยู่ใน DB |
| D-83 | ข้อมูลครบของหน้าต่างน้ำสูญหาย (แทนส่วนหนึ่งของ D-78) | มิเตอร์/ถังทุกตัวต้องมีข้อมูลห่างหัวและท้ายหน้าต่างไม่เกิน 2 นาที ไม่ใช่แค่มีสักแถว · ประเมินหน้าต่างย้อนหลังจะไม่เปิด alert ที่จบหลังเวลานั้นกลับมา | เทสรวมจับได้: หลังเปิดระบบ 9 นาที หน้าต่าง 60 นาทีมีข้อมูลแค่ท้ายช่วง (และเทสสั่งงานฉีดระดับถังเอง) worker เปิด alert วิกฤต 45.86% ปลอม · กติกา "มีสักแถว" ปล่อยอุปกรณ์ที่เงียบไปกลางหน้าต่างผ่าน |
| D-84 | archive ข้อมูลดิบ | worker ทุกคืน 01:30 เขียน Parquet (zstd) หนึ่งไฟล์ต่อ (ตาราง, วันตามเวลาโรงงาน) แบบ hive `<table>/date=YYYY-MM-DD/` · อ่านไฟล์กลับนับแถวก่อนบันทึก `archive_manifest` · ไล่วันตาม chunk ที่มีอยู่จริง (ข้อมูลเก่าแค่ไหนก็ได้ archive) · วันล่าสุด 14 วัน และวันที่กำลังจะถูกลบ ถ้าจำนวนแถวเปลี่ยนเขียนใหม่ทั้งวัน · วันที่เพิ่งจบไม่ถึง 2 ชม. ยังไม่ archive | ทีม AI อ่านทั้งโฟลเดอร์ด้วย `pandas.read_parquet` ได้ทันที · ตัดตามวันของโรงงานให้ตรงกับรายงาน ไม่ใช่ chunk UTC · รุ่นแรกนับถอยหลัง 400 วันจากวันนี้ ข้อมูลเก่ากว่านั้นไม่มีวัน archive แล้วถูกกันไม่ให้ลบตลอดไป (เจอตอนรันกับข้อมูลทดสอบปี 2025) |
| D-85 | retention | ข้อมูลดิบ: worker ลบ chunk ที่เก่ากว่า `maintenance.dataRetentionDays` (ขั้นต่ำ 30 วัน) เองหลังตรวจว่าทุกวันที่ chunk ครอบมี manifest และจำนวนแถวตรง — ไม่ใช้ retention policy · aggregate: 5 นาที 90 วัน · 1 ชม. 2 ปี · 1 วัน ถาวร (policy ใน `09_archive.sql`) | policy ของ TimescaleDB ลบตามเวลาอย่างเดียว ไม่รู้ว่า archive สำเร็จหรือยัง · ⚠️ PROMPT_06 เขียน raw 30 วัน แต่ค่าตั้งต้นใน seed คือ 365 วัน — ใช้ค่าตั้งให้ admin ปรับได้ ต้องให้ทีมยืนยันตัวเลข |
| D-86 | สำรองฐานข้อมูล | worker รัน `pg_dump -Fc` (client 17 ใน image ของ worker) ตาม `maintenance.backupTime` เมื่อ `backupEnabled` · ปลายทาง `BACKUP_HOST_DIR` ที่ host mount SMB ไว้แล้ว bind เข้ามา · ตรวจไม่ว่าง + `pg_restore --list` มีตารางหลัก + สัดส่วนไฟล์ต่อขนาดฐานข้อมูลไม่ต่ำกว่าครั้งก่อนเกินครึ่ง (เทียบขนาดไฟล์ตรง ๆ ถูกปฏิเสธผิดหลัง retention ลบ 120 chunk) · เก็บ 14 ไฟล์ · ผลลง `backup_runs` · ล้มเหลวไม่ลองซ้ำในวันเดียวกัน | container mount SMB เองต้องได้สิทธิ์พิเศษและเก็บรหัส SMB · กู้คืนต้อง `timescaledb_pre_restore()` → `pg_restore` → `timescaledb_post_restore()` ด้วย image TimescaleDB รุ่นเดียวกัน (เกณฑ์ข้อ 9 ทดสอบจริง) |
| D-87 | งานกลางคืนของ worker | archive และ backup รันใน thread แยกพร้อม connection ของตัวเองทีละงาน · เปิด worker หลัง 01:30 = archive ทันทีหนึ่งรอบ | dump ใช้เวลาหลายนาทีถึงหลายชั่วโมง ลูปหลักต้องแตะ heartbeat และเรนเดอร์ไฟล์ส่งออกต่อได้ · archive ซ้ำไม่เสียหาย (ข้ามวันที่มี manifest) |
| D-88 | bundle ขึ้นเครื่องจริง | `scripts/bundle.sh` (make bundle) ตรวจว่าทุก image ใน `docker-compose.yml` ปักด้วย digest หรือ tag เวอร์ชัน → build → pull → `docker save` เป็น tar + sha256 · ไม่รวม simulator · `scripts/install.sh` (make install) ตรวจ sha256 → load → ตรวจว่ามีครบทุก image → `up --no-build --pull never --wait` | ปลายทางไม่มีเน็ต ขาด image ต้องล้มทันทีพร้อมชื่อ ไม่ใช่ค้างรอ pull · ⚠️ ยังไม่ได้ซ้อมบนเครื่องที่ตัดเน็ตจริง (เกณฑ์เฟส 7 ข้อ 8–9) โดยเฉพาะ image ที่อ้างด้วย digest หลัง `docker load` |
| D-89 | CI ของ backend | job ใหม่ `check-backend` (ruff · unit test · `docker compose config` ด้วย `.env.example`) ไม่แตะ job `check` · แพ็กเกจของเทสอยู่ `tests/requirements.txt` ใช้ร่วมกับ `make venv` | job `check` เป็น required check ของ branch protection · ลองใน venv เปล่าตามขั้นของ CI แล้วเจอว่า `conftest.py` ต้องใช้ httpx ที่ไม่เคยอยู่ในรายการ (เครื่องพัฒนามีติดมาเอง) · ⚠️ เจ้าของ repo ต้องเพิ่ม `check-backend` เป็น required check |
| D-90 | สลับหน้าบ้านจาก mock เป็น API (PROMPT_07 งาน 1–3) | **ยังไม่ทำ — หยุดตาม RUNBOOK จุดที่ต้องหยุดถาม ข้อ 2, 3, 5** | ต้องเขียนทับ `lib/services/` 20 ไฟล์และลบ `lib/mock/` แต่ `pages.yml` build เดโม GitHub Pages จาก mock (ลบแล้วเดโมพังทันทีที่ merge เข้า main) · `scripts/check-series.mjs` import `lib/mock/metric-series.ts` · `lib/services` เป็นสัญญาของทีมหน้าบ้าน — ทางเลือกอยู่ใน contract notes ข้อ 53 |
