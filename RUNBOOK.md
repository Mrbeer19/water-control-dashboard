# RUNBOOK.md — โหมดทำงานต่อเนื่องด้วยตัวเอง

ไฟล์นี้กำหนดวิธีที่ Claude Code ต้องทำงานเมื่อได้รับคำสั่งให้เดินหน้าเองหลายเฟส

---

## ลูปการทำงานของแต่ละเฟส

ทุกเฟสต้องเดินครบ 6 ขั้นนี้ ห้ามข้ามขั้นไหน

### ขั้น 1 — สำรวจก่อนเขียน
- อ่าน `CLAUDE.md` และสเปกของเฟสนั้นใน `PROMPTS.md`
- **ตรวจก่อนเสมอว่าไฟล์/type/ฟังก์ชันที่จะสร้างมีอยู่แล้วหรือยัง** (`ls`, `grep`)
- ถ้ามีอยู่แล้วและทำหน้าที่เดียวกัน → ต่อยอดของเดิม ห้ามเขียนทับ ห้ามสร้างซ้ำ
- สรุปสั้น ๆ ว่าจะแตะไฟล์ไหนบ้าง เพิ่มอะไร แก้อะไร แล้วค่อยลงมือ

### ขั้น 2 — ลงมือทำ
- ทำตามสเปกของเฟสนั้น ไม่ทำเกิน ไม่เริ่มเฟสถัดไป
- ถ้าระหว่างทางพบว่าสเปกขัดกับโค้ดที่มีอยู่ → หยุด ไปดู "จุดที่ต้องหยุดถาม" ด้านล่าง

### ขั้น 3 — ตรวจด้วยตัวเอง (Self-review)
รันเช็คลิสต์นี้ทุกครั้ง แก้ที่เจอเองทันที ไม่ต้องรอถาม

```
[ ] npx tsc --noEmit ผ่าน ไม่มี error
[ ] npm run build ผ่าน
[ ] npm run lint ผ่าน (ถ้ามี)
[ ] ไม่มี any ในโค้ดที่เพิ่งเขียน
[ ] ไม่มี component ไหนเรียก lib/mock/ โดยตรง — ต้องผ่าน lib/services/ ทั้งหมด
    ตรวจด้วย: grep -rn "lib/mock" components/ app/
[ ] ไม่มีการเรียก external URL / CDN / Google Fonts / weather API
    ตรวจด้วย: grep -rniE "https?://(?!localhost)" app/ components/ lib/
[ ] ทุกฟังก์ชันใน lib/services/ ที่เพิ่มใหม่ มีคอมเมนต์ TODO(backend): ระบุ method + endpoint
[ ] ทุกหน้าที่ทำในเฟสนี้มี loading state และ empty state
[ ] ข้อความบน UI เป็นภาษาไทย มีคีย์ครบทั้ง th และ en ใน lib/i18n/
[ ] ตัวเลขทุกจุดจัดรูปแบบผ่าน util กลาง ไม่มี toFixed() กระจายใน component
[ ] เปิด dark mode แล้วอ่านออกทุกจุด ไม่มีตัวอักษรสีขาวบนพื้นขาว
[ ] responsive: ตรวจ layout ที่ 375px และ 1920px ไม่ล้นจอ ไม่มี horizontal scroll
[ ] ไม่มี type ที่ทำหน้าที่ซ้ำกับ type เดิมใน lib/types.ts
[ ] ไม่ได้แก้ไฟล์ที่ไม่เกี่ยวกับเฟสนี้ — ตรวจด้วย git status
```

### ขั้น 4 — ตรวจรอบสอง แบบมองต่างมุม
อ่านโค้ดที่เพิ่งเขียนใหม่อีกรอบ โดยสมมติว่าเป็นคนอื่นมารีวิว แล้วตอบ 4 คำถามนี้เป็นข้อความ:

1. ถ้า service layer คืน array ว่าง / null / field หาย หน้านี้พังไหม
2. ถ้าข้อมูลมี 8 โซนกลายเป็น 20 โซน layout ยังใช้ได้ไหม
3. มีตรงไหนที่ hardcode ค่าที่ควรมาจาก settings หรือ service layer ไหม
4. ทีมหลังบ้านอ่าน TODO(backend) ที่เขียนไว้แล้วรู้ไหมว่าต้องทำ endpoint อะไร ส่ง JSON หน้าตาไหน

พบปัญหาจาก 4 ข้อนี้ → แก้เองเลย แล้ววนกลับไปขั้น 3 อีกรอบ

### ขั้น 5 — บันทึก
```
git add -A
git commit -m "Phase X: <สรุปสั้น ๆ>"
```
ถ้ายังไม่ได้ `git init` ให้ทำก่อนเป็นอย่างแรก

### ขั้น 6 — รายงานแล้วไปต่อ
พิมพ์สรุปสั้น ๆ ไม่เกิน 10 บรรทัด:
- เฟสไหนเสร็จ ทำอะไรไปบ้าง
- เพิ่ม type อะไรใน `lib/types.ts` / เพิ่มฟังก์ชันอะไรใน `lib/services/`
- เจออะไรในขั้น 4 แล้วแก้ยังไง
- ข้อสังเกตที่ผมควรรู้ (ถ้ามี)

แล้วขึ้นเฟสถัดไปทันทีโดยไม่ต้องรอคำสั่ง เว้นแต่ติดเงื่อนไขใน "จุดที่ต้องหยุดถาม"

---

## จุดที่ต้องหยุดถาม (หยุดทันที อย่าเดา)

หยุดและถามผมเมื่อเจอข้อใดข้อหนึ่งนี้:

1. **ต้องแก้หรือ rename type/field ที่มีอยู่แล้วใน `lib/types.ts`**
   (เพิ่ม field ใหม่แบบ optional ทำได้เลย ไม่ต้องถาม)
2. **ต้องลบไฟล์ ลบฟังก์ชัน หรือเขียนทับไฟล์ที่มีเนื้อหาอยู่แล้ว**
3. **สเปกใน `PROMPTS.md` ขัดกับโค้ดที่มีอยู่จริง** — ให้รายงานว่าขัดตรงไหน เสนอทางเลือก แล้วรอ
4. **ต้องเพิ่ม dependency ใหม่นอกเหนือจาก stack ใน `CLAUDE.md`**
5. **ต้องตัดสินใจเรื่องที่กระทบสัญญากับทีมอื่น** (ทีม AI หรือทีมหลังบ้าน) เช่น หน่วย สเกล ชื่อ endpoint
6. **`npm run build` หรือ `tsc` ไม่ผ่าน และแก้เองแล้ว 3 ครั้งยังไม่ผ่าน**
7. **สเปกกำกวมจนมีทางตีความเกิน 1 แบบ และแต่ละแบบให้ผลต่างกันมาก**

เวลาหยุดถาม ให้เขียนแบบนี้:
```
หยุดที่ Phase X — <เรื่องที่ติด>
สถานการณ์: <อธิบาย 2-3 บรรทัด>
ทางเลือก:
  (ก) ... ผลที่ตามมา ...
  (ข) ... ผลที่ตามมา ...
แนะนำ: (ก) เพราะ ...
งานที่ทำไปแล้วในเฟสนี้: <ระบุ> — commit ไว้แล้ว/ยังไม่ commit
```

---

## กฎครอบทั้งหมด

- **ของที่มีอยู่ชนะสเปกเสมอ** ถ้าโค้ดกับ `PROMPTS.md` ไม่ตรงกัน ให้แก้ `PROMPTS.md` ตามโค้ด ไม่ใช่รื้อโค้ด
- ห้ามเริ่มเฟสถัดไปถ้าเฟสปัจจุบัน build ไม่ผ่าน
- ห้ามทำงานข้ามเฟส ถ้าเห็นว่าเฟสหลังควรทำก่อน ให้บอกแล้วรอ
- ห้าม refactor โค้ดเฟสเก่าที่ทำงานได้อยู่แล้ว เว้นแต่จำเป็นจริง ๆ และบอกก่อน
- ถ้า context ใกล้เต็ม ให้ commit งานที่ทำอยู่ แล้วบอกผมว่าให้ `/clear` ตรงไหน จะได้กลับมาต่อถูกจุด
- `PROMPTS.md` และ `docs/AI_CONTRACT.md` ต้องสะท้อนโค้ดจริงเสมอ อัปเดตทุกครั้งที่ของจริงเปลี่ยน
