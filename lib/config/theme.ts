/**
 * สีของแบรนด์ Kasetphand — จุดเดียวในโค้ดที่เก็บค่า hex
 *
 * ★ ทุกค่ามาจากบันไดสีใน docs/BRANDING_SPEC.md ข้อ 3.1 ห้ามคิดสีเอง ห้ามใช้ palette ของ Tailwind
 *   ถ้าต้องการเฉดที่ไม่มีในนี้ ให้หยุดถามเจ้าของโปรเจกต์ อย่าเติมเอง
 *
 * ค่าขั้น 500 ของทุกสีเป็น hex ทางการที่พิมพ์อยู่ในเอกสาร CI
 * ขั้นอื่น sampled from CI p.13/p.17 — confirm with brand owner
 * (ตรวจแล้วตรงกับพิกเซลในไฟล์ PDF ทุกขั้น ดู docs/DESIGN_PLAN.md ข้อ 1)
 *
 * component ทั่วไป **ห้าม import ไฟล์นี้** ให้ใช้ semantic token ผ่าน class ของ Tailwind แทน
 * ไฟล์นี้มีไว้สำหรับจุดที่ใส่ CSS class ไม่ได้จริง ๆ เท่านั้น (เช่น metadata ของ Next.js)
 */

/** บันไดสีหลักตาม CI — ข้อ 3.1 */
export const CI = {
  cinnabar: {
    900: '#624D4A',
    800: '#92584A',
    700: '#BC5242',
    500: '#E9242B',
    300: '#F05D49',
    200: '#F5856D',
    100: '#F7AB94',
  },
  argent: {
    900: '#515558',
    800: '#6D6C71',
    700: '#7E7D82',
    500: '#888888',
    300: '#9C9C9C',
    200: '#B4B0B1',
    100: '#CBC7C8',
  },
  royalNavyBlue: {
    900: '#4B4E5F',
    800: '#536281',
    700: '#44699D',
    500: '#026BB5',
    300: '#4E80BF',
    200: '#8290B5',
    100: '#A5AECF',
  },
  marigold: {
    900: '#625B4B',
    800: '#937A52',
    700: '#C29345',
    500: '#F2A81E',
    300: '#F7B94C',
    200: '#FBC77B',
    100: '#FDD8A3',
  },
  greenRevolution: {
    900: '#48534B',
    800: '#526E57',
    700: '#428154',
    500: '#009148',
    300: '#4CA062',
    200: '#7BB481',
    100: '#A3C8A7',
  },
} as const;

/** สีกลางที่ CI ระบุไว้ — ข้อ 3.1 */
export const NEUTRAL = {
  /** พื้นหน้าในโหมดสว่าง */
  lynxWhite: '#F7F7F7',
  /** พื้นการ์ด — ข้อยกเว้นเดียวที่ไม่ได้อยู่ในบันไดสี */
  white: '#FFFFFF',
} as const;

/**
 * ขั้นต่อขยายของ Argent — ข้อ 3.6 ทางเลือก (ข)
 *
 * ★ สามค่านี้ไม่อยู่ใน CI คง H/S ของบันได Argent ไว้ ขยับเฉพาะ L
 *   (94.5% สำหรับพื้นหน้าโหมดสว่าง · 22% และ 13% สำหรับโหมดมืด)
 *   ใช้ได้เฉพาะเป็น "พื้น" เท่านั้น ห้ามใช้กับข้อความ ไอคอน เส้นขอบ หรือสีชุดข้อมูล
 *   ต้องแจ้งเจ้าของแบรนด์ (open question ใน docs/DESIGN_PLAN.md)
 */
export const ARGENT_EXTENDED = {
  /**
   * พื้นหน้าในโหมดสว่าง — hsl(345 3.7% 94.5%)
   * ★ เพิ่มเพราะ Lynx White (#F7F7F7) กับพื้นการ์ดสีขาวต่างกันแค่ 1.07 : 1
   *   การ์ดเลยดูจมไปกับพื้น ไม่มีลำดับชั้นให้ตาจับ (ข้อ 6.2)
   *   ขั้นนี้ให้ 1.14 : 1 ซึ่งเห็นขอบการ์ดชัดขึ้นโดยที่ข้อความ Argent-800
   *   ยังได้ 4.58 : 1 บนพื้นนี้ (ผ่าน 4.5)
   */
  50: '#F1F0F1',
  /** พื้นการ์ดในโหมดมืด */
  950: '#36383A',
  /** พื้นหน้าในโหมดมืด */
  1000: '#202123',
} as const;

/**
 * สีแบรนด์ขององค์กรอื่น — ข้อยกเว้นเดียวของกฎ "ทุก hex ต้องมาจากตาราง 3.1" (ข้อ 3.7)
 *
 * ★ ใช้ได้ที่ components/alerts/line-preview-card.tsx ไฟล์เดียวเท่านั้น
 *   ห้ามใช้เป็นสีสถานะ สีชุดข้อมูล หรือสีตกแต่งที่อื่น
 */
export const thirdParty = {
  /** สีแบรนด์ของ LINE ใช้ให้ตัวอย่างข้อความเหมือนของจริง */
  line: '#06C755',
} as const;

/**
 * สีของแถบ UI ของเบราว์เซอร์ — ต้องตรงกับ --background ของแต่ละโหมดใน app/globals.css
 * ใส่ใน metadata ของ Next.js ซึ่งเป็น JS ล้วน ใช้ CSS variable ไม่ได้
 */
export const themeColor = {
  light: NEUTRAL.lynxWhite,
  dark: ARGENT_EXTENDED[1000],
} as const;
